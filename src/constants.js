'use strict';

const SUBMISSION_API = '/submission-svc/api/v1.0';

const serviceConfig = {
  serviceName: 'submission-svc',
  HOST: 'localhost',
  PORT: 5400,
  PROTOCOL: 'http',
};

export { SUBMISSION_API, serviceConfig };
